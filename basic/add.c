#include<stdio.h>
int main()
{
    int x,y,z;
    printf("enter the number x :");
    scanf("%d", &x);
    printf("enter the number y :");
    scanf("%d", &y);
    z=x+y;
    printf("sum of the x + y : %d", z);
    return 0;
}